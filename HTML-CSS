<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Point Management & Visit Tracking</title>
    <style>
        body { font-family: Arial, sans-serif; background-color: #737373; }
        h1 { text-align: center; }
        .container { max-width: 400px; margin: auto; border: 2px solid #000000; padding: 20px; background-color: #c5cbf8; }
        button { padding: 10px; background-color: #007bff; color: white; border: none; cursor: pointer; }
        button:hover { background-color: #4a4646; }
        .delete-btn { background-color: red; color: white; }
        .delete-btn:hover { background-color: darkred; }
        .points-list-item { display: flex; justify-content: space-between; align-items: center; }
    </style>
</head>
